# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5912/worklio?sslmode=disable

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production

# Server Port
PORT=8080

# SMTP Configuration (Optional - for email verification/password reset)
# If not configured, verification tokens will be logged to console
SMTP_HOST=
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
SENDER_EMAIL=noreply@yourdomain.com
SENDER_NAME=FacturMe

# Application URL (for email links)
APP_URL=http://localhost:5173

# Frontend API URL
VITE_API_URL=http://localhost:8080
